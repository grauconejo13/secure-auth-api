# Runtime
NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Database
MONGODB_URI=mongodb://localhost:27017/secure_auth_api

# JWT access tokens
# Generate a local value with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# Replace this example. Never commit a real production value.
JWT_ACCESS_SECRET=replace-this-with-a-random-secret-of-at-least-32-characters
